import { AuctionSearchPipe } from './auction-search.pipe';

describe('AuctionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AuctionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
